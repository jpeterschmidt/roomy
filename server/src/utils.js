const EIGHTEEN_YEARS_IN_DAYS = Math.ceil(365.25 * 18)
const TWENTY_ONE_YEARS_IN_DAYS = Math.ceil(365.25 * 21)
const TWO_YEARS_IN_DAYS = 365 * 2
const NOW = Date.now()
const HOUSING = 'housing'
const BATHROOM = 'bathroom'

module.exports = {
    EIGHTEEN_YEARS_IN_DAYS,
    TWENTY_ONE_YEARS_IN_DAYS,
    TWO_YEARS_IN_DAYS,
    NOW,
    HOUSING,
    BATHROOM
}